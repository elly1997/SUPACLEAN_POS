# Set to 1 or true to use the frozen pre–design-system shell (client/src/components/layout-legacy/).
# Remove or leave unset for the default SUPACLEAN design-system POS shell.
# REACT_APP_LEGACY_POS_SHELL=1
